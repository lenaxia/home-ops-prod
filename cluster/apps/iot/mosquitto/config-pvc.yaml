apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-data
  labels:
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
      - ReadWriteMany
  storageClassName: longhorn
  resources:
      requests:
          storage: 128Mi

