apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: config-plex-0
    labels:
      app: plex
      snapshot.home.arpa/enabled: "true"
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 60Gi

