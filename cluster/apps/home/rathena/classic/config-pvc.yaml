apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: rathena-classic-volume
    labels:
      app: rathena-classic
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 3Gi

