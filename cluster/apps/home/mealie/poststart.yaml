apiVersion: v1
kind: ConfigMap
metadata:
  name: mealie-poststart-sh
  namespace: home
data:
  poststart.sh: |
    #!/bin/sh
    apt update
    apt install -y --no-install-recommends libldap-2.4-2 
    sed -i 's/objectClass=user/objectClass=posixAccount/' /app/mealie/core/security/security.py 
