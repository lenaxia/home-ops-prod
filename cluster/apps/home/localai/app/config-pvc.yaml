apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: localai-volume
  labels:
    app: localai
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 30Gi

