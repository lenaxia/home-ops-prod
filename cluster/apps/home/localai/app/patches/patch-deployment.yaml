apiVersion: apps/v1
kind: Deployment
metadata:
  name: localai
  namespace: home
spec:
  template:
    spec:
      containers:
      - name: localai
        volumeMounts:
        - mountPath: /tmp/generated/images
          name: generate-emptydir
      volumes:
      - name: generate-emptydir
        emptyDir: {}

