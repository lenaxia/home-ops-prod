apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: babybuddy-pandaria-volume
    labels:
      app: babybuddy-pandaria
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 256Mi

