apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: wallabag-volume
    labels:
      app: wallabag
spec:
    accessModes:
        - ReadWriteOnce
    storageClassName: longhorn
    resources:
        requests:
            storage: 256Mi

