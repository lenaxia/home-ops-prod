---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-socks-pvc
  namespace: test
  annotations:
    volumeType: local
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  #storageClassName: longhorn
  resources:
    requests:
      storage: 512Mi
