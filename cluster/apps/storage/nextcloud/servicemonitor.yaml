apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: doesNotMatter
  namespace: storage
spec:
  endpoints:
  - interval: 1m
    path: /metrics
    port: metrics
    scrapeTimeout: 10s
